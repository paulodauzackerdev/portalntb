FROM node:22-bookworm-slim

WORKDIR /app
COPY package*.json ./
RUN npm ci

# Cria diretório src mínimo para o Next.js não reclamar
# (será sobrescrito pelo volume mount em dev)
RUN mkdir -p src/app src/lib

EXPOSE 3000

CMD ["npm", "run", "dev"]
